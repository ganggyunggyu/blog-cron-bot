[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "pm2-runtime start ecosystem.railway.config.cjs"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ALWAYS"
